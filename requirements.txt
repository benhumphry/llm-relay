flask>=3.0.0
anthropic>=0.40.0
openai>=1.0.0
gunicorn>=21.0.0
pyyaml>=6.0.0
requests>=2.31.0
httpx>=0.27.0  # HTTP client for provider APIs

# v2.0: Database and admin
sqlalchemy>=2.0.0
bcrypt>=4.0.0
cryptography>=41.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (optional - for external DB)

# v3.x: Context features (Smart Cache, Augmentor, RAG)
chromadb>=0.4.0  # Vector database for semantic search/caching
sentence-transformers>=2.2.0  # Local embeddings for Smart RAG
docling>=2.0.0  # Document parsing for Smart RAG (PDF, DOCX, etc.)
tesserocr>=2.6.0  # Tesseract OCR bindings for Docling
pypdf>=4.0.0  # Fallback PDF text extraction when Docling fails
apscheduler>=3.10.0  # Background scheduling for RAG indexing
trafilatura>=1.6.0  # Web content extraction for Smart Augmentors
mcp>=1.0.0  # Model Context Protocol client for MCP integrations (v1.6)
uv>=0.5.0  # Fast Python package installer/runner for MCP servers (uvx)
